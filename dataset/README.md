# Data directory

This repository expects datasets to be placed under `dataset/` but **does not** version any dataset content.

- RealEstate10K goes in `dataset/re10k/`.
- See `docs/DATA.md` for the expected layout and preprocessing notes.
